# Secrets
.env
.env.local

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# Node / Next.js
node_modules/
.next/
out/
.turbo/

# Local data caches (gitignored — rebuilt at runtime)
data/duckdb_cache/
data/*.parquet

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

terraform/terraform.tfvars

# Terraform state + provider binaries (state contains secrets; binaries are huge and rebuilt by `terraform init`)
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*

# Lambda packages — rebuilt by `uv run backend/package_agents.py`
backend/**/*.zip